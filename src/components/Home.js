import React from 'react'

const Home = () => {
    return (
        <div> <h4>What is React context?</h4>
            <p>React Context is a way to manage state globally.</p>
            <p>React context allows us to share data (state) across our components more easily.</p>
            <p>It can be used together with the useState Hook to share state between deeply nested components </p>
        </div>
    )
}
export default Home